# macOS
.DS_Store

# Xcode
xcuserdata/
DerivedData/
build/
*.xcresult

# Code coverage counters, regenerated by every instrumented run
*.profraw

# Swift Package Manager
.build/
.swiftpm/

# Throwaway app bundles built to capture the README screenshots, kept out of
# the way of the real build products
.codex-runtime/
